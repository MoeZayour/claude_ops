<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Rebuild Monthly Snapshots (nightly at 2 AM) -->
        <record id="ir_cron_rebuild_monthly_snapshots" model="ir.cron">
            <field name="name">OPS: Rebuild Monthly Financial Snapshots</field>
            <field name="model_id" ref="model_ops_matrix_snapshot"/>
            <field name="state">code</field>
            <field name="code">model.cron_rebuild_monthly_snapshots()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>

        <!-- Rebuild Weekly Snapshots (Sundays at 3 AM) -->
        <record id="ir_cron_rebuild_weekly_snapshots" model="ir.cron">
            <field name="name">OPS: Rebuild Weekly Financial Snapshots</field>
            <field name="model_id" ref="model_ops_matrix_snapshot"/>
            <field name="state">code</field>
            <field name="code">model.cron_rebuild_weekly_snapshots()</field>
            <field name="interval_number">7</field>
            <field name="interval_type">days</field>
            <field name="active" eval="False"/>
            <field name="priority">10</field>
        </record>

        <!-- Rebuild Quarterly Snapshots (monthly at 4 AM on 1st) -->
        <record id="ir_cron_rebuild_quarterly_snapshots" model="ir.cron">
            <field name="name">OPS: Rebuild Quarterly Financial Snapshots</field>
            <field name="model_id" ref="model_ops_matrix_snapshot"/>
            <field name="state">code</field>
            <field name="code">model.cron_rebuild_quarterly_snapshots()</field>
            <field name="interval_number">30</field>
            <field name="interval_type">days</field>
            <field name="active" eval="False"/>
            <field name="priority">15</field>
        </record>
    </data>
</odoo>
