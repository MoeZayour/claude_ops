<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================================
         OPS MATRIX ACCOUNTING - REPORT MENU

         Phase 1 Cleanup: Removed broken and duplicate menu items.
         - REMOVED: Report Templates (model doesn't exist)
         - REMOVED: Excel Export (model doesn't exist)
         - REMOVED: Consolidated Reporting (models don't exist yet)

         Kept: Financial Reports Wizard (basic, until Phase 2)
         ================================================================ -->

    <!-- Financial Reports Wizard Action -->
    <record id="action_ops_financial_reports_wizard" model="ir.actions.act_window">
        <field name="name">Financial Reports</field>
        <field name="res_model">ops.financial.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>

    <!-- Financial Reports Wizard Action (alternate ID for compatibility) -->
    <record id="action_ops_financial_report_wizard_new" model="ir.actions.act_window">
        <field name="name">Financial Reports</field>
        <field name="res_model">ops.financial.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Menu Item under Accounting > Reporting -->
    <menuitem id="menu_ops_financial_reports"
              name="Financial Reports"
              parent="account.menu_finance_reports"
              action="action_ops_financial_report_wizard_new"
              sequence="10"
              groups="account.group_account_manager"/>

</odoo>
