<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="apply_report_template_wizard_form_view" model="ir.ui.view">
            <field name="name">apply.report.template.wizard.form</field>
            <field name="model">apply.report.template.wizard</field>
            <field name="arch" type="xml">
                <form string="Apply Report Template">
                    <group>
                        <field name="template_id"/>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="branch_id" options="{'no_create': True}"/>
                        <field name="business_unit_id" options="{'no_create': True}"/>
                    </group>
                    <footer>
                        <button name="action_apply_template" string="Apply Template" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
