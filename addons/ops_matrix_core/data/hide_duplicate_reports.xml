<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="0">
    <!--
    OPS inherits native templates directly via XPath.
    No report bindings need to be hidden.

    We only deactivate the Quote Builder "raw" variant to prevent
    it from appearing as a separate option alongside the styled report.
    -->
    <record id="sale_pdf_quote_builder.action_report_saleorder_raw" model="ir.actions.report">
        <field name="binding_model_id" eval="False"/>
    </record>
</odoo>
